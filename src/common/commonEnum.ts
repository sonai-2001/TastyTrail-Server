export enum RoleEnum {
  ADMIN = 'admin',
  USER = 'user'
}

export enum checkEnum {
    CHECK = 'check',
}


export enum statusEnum {
  ACTIVE='active',
  INACTIVE='inactive'
}

export enum friendStatus{
  PENDING='pending',
  ACCEPTED='accepted',
  REJECTED = 'REJECTED'
}